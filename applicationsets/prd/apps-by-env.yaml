apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps-by-env-prd
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - git:
        repoURL: https://github.com/jgpjuniorj/gitops.git
        revision: main
        directories:
          - path: envs/prd/gestao-financeira/overlay
  template:
    metadata:
      name: '{{ $app := index .path.segments 2 }}{{ if hasSuffix "-prd" $app }}{{ $app }}{{ else }}{{ $app }}-prd{{ end }}'
    spec:
      project: '{{ if eq (index .path.segments 2) "cloudflared" }}project-core{{ else }}project-negocio{{ end }}'
      source:
        repoURL: https://github.com/jgpjuniorj/gitops.git
        targetRevision: main
        path: '{{ .path.path }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{ index .path.segments 2 }}'
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/replicas
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
